#pragma once

// rust_clock.h depends on rust_fileman.h, but we cannot add it
// to rust_clock.h  It is solved by using this file instead of rust_clock.h

// must be included first
#include "rust_fileman.h"
// must be included second
#include "rust_clock.h"
