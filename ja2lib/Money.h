#ifndef __MONEY_H
#define __MONEY_H

#include "LeanTypes.h"

// Get money balance (what is shown on the laptop screen).
i32 MoneyGetBalance();

// Get money balance (what is shown on the laptop screen).
void MoneySetBalance(i32 value);

#endif
